require "spec_helper"

describe Capistrano::Docker::Copy do
  it "has a version number" do
    expect(Capistrano::Docker::Copy::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
